{
  "name": "quorum",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clear": "npm run clear --workspace=@quorum/web"
  },
  "devDependencies": {
    "turbo": "^2.0.11"
  },
  "dependencies": {
    "@stackframe/stack": "^2.8.108"
  }
}
